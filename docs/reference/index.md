# Reference

Lookups for integrators: environment variables, section taxonomy, Python API, and HTTP OpenAPI. OpenBB Workspace discovery routes (`/widgets.json`, `/openbb/company`, etc.) are included in the generated HTTP endpoint reference; see also {doc}`../guides/openbb/index`.

```{toctree}
:maxdepth: 2

environment-variables
section-taxonomy
versioning
score-catalog
python/index
http/openapi
http/endpoints
```
