# Python API Reference

Public Python modules. High-level helpers are also exported from `disclosure_alpha` (see `score_filing_ticker`, `score_filing_html`, etc.).

```{toctree}
:maxdepth: 1

pipeline
cli
section_extractor
text_metrics
diff_engine
deterministic_scoring
dictionaries
edgar
mcp
api
```
